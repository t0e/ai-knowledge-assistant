# Project Screenshots Guidelines

Place high-resolution screenshots here for inclusion in the repository README:

1. `documents_overview.png` — Document management table showing uploaded PDF, Markdown, and Website sources with `Ready` status badges.
2. `website_ingestion.png` — Ingesting a single-page URL with real-time SSRF validation.
3. `chat_streaming_citations.png` — Streaming AI conversational RAG response with inline source citation markers `[1]`.
4. `citation_popover.png` — Hovering over or clicking a citation badge showing the exact excerpt, source document, and section heading.
5. `evaluation_terminal.png` — Terminal execution of Phase 9 benchmark runner (`python -m apps.api.src.evaluation.cli --run`).
